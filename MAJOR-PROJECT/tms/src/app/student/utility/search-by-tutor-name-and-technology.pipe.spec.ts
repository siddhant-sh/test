import { SearchByTutorNameAndTechnologyPipe } from './search-by-tutor-name-and-technology.pipe';

describe('SearchByTutorNameAndTechnologyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTutorNameAndTechnologyPipe();
    expect(pipe).toBeTruthy();
  });
});
